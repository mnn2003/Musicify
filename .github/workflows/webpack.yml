name: NodeJS with Webpack

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrixThe failure occurred because the workflow encountered an interactive prompt asking whether to install `webpack-cli`, which it couldn't handle in a non-interactive environment. To solve this, you can ensure `webpack-cli` is installed without prompting.

Here's the:
        node-version: [18.x, 20.x, 22.x]

    steps:
 updated workflow file:

```yaml
name: NodeJS with Webpack

on:
  push:
    branches: [ "main" ]
  pull_request    - uses: actions/checkout@v4

    - name: Use Node:
    branches: [ "main" ]

jobs:
  build:
    runs.js ${{ matrix.node-version }}
      uses: actions/setup-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    steps:
   -node@v4
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
 - uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup      run: |
        npm install
        npm install --node@v4
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: |
        npm install
        npm install -D webpack-cli

    - name: Build
      run: npx webpack
D webpack-cli

    - name: Build
      run: |
        n```

In this updated workflow, the `Install dependencies`px webpack
